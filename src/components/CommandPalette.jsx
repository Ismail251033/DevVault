import { useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom'
const actions = [['Dashboard','/dashboard'],['Projects','/projects'],['New Project','/projects?new=1'],['Tasks','/tasks'],['New Task','/tasks?new=1'],['Snippets','/snippets'],['New Snippet','/snippets?new=1'],['Notes','/notes'],['New Note','/notes?new=1'],['Commands','/commands'],['Analytics','/analytics'],['Settings','/settings']]
export default function CommandPalette({ onClose }) { const [query,setQuery]=useState(''); const [index,setIndex]=useState(0); const nav=useNavigate(); const items=actions.filter(a=>a[0].toLowerCase().includes(query.toLowerCase())); useEffect(()=>{const f=e=>{if(e.key==='Escape')onClose(); if(e.key==='ArrowDown')setIndex(i=>Math.min(i+1,items.length-1)); if(e.key==='ArrowUp')setIndex(i=>Math.max(i-1,0)); if(e.key==='Enter'&&items[index]){nav(items[index][1]);onClose()}};addEventListener('keydown',f);return()=>removeEventListener('keydown',f)},[items,index]); return <div className="palette-backdrop" onMouseDown={onClose}><section className="palette" onMouseDown={e=>e.stopPropagation()}><input autoFocus placeholder="Type a command…" value={query} onChange={e=>{setQuery(e.target.value);setIndex(0)}}/>{items.map((a,i)=><button className={i===index?'selected':''} key={a[0]} onClick={()=>{nav(a[1]);onClose()}}><span>{a[0]}</span><small>Go to</small></button>)}</section></div> }
