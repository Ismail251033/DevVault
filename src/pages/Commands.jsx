import { useState } from 'react';import { Copy,Search } from 'lucide-react';import { useToast } from '../components/Toast';const commands=[['Git','git status','Show working tree status'],['Git','git switch -c feature/name','Create and switch branch'],['npm','npm run build','Build production assets'],['Windows','Get-ChildItem','List files in PowerShell'],['Node.js','node --version','Show Node version'],['React','useEffect(() => {}, [])','Run an effect on mount'],['Vite','npm run dev','Start local development'],['Supabase','supabase login','Authenticate Supabase CLI']];export default function Commands(){const [q,setQ]=useState(''),[cat,setCat]=useState('All'),toast=useToast();const cats=['All',...new Set(commands.map(x=>x[0]))];const xs=commands.filter(x=>(cat==='All'||x[0]===cat)&&x.join(' ').toLowerCase().includes(q.toLowerCase()));return <><section className="page-heading"><div><p className="eyebrow">REFERENCE</p><h1>Command center</h1><p>Your practical developer command reference.</p></div></section><div className="toolbar"><label className="search"><Search size={17}/><input placeholder="Search commands…" value={q} onChange={e=>setQ(e.target.value)}/></label><select value={cat} onChange={e=>setCat(e.target.value)}>{cats.map(c=><option key={c}>{c}</option>)}</select></div><div className="command-grid">{xs.map(([category,command,description])=><article className="command-card" key={command}><small>{category}</small><code>{command}</code><p>{description}</p><button onClick={()=>navigator.clipboard.writeText(command).then(()=>toast('Command copied.'))}><Copy size={15}/> Copy</button></article>)}</div></>}
