import { useEffect,useState } from 'react';import { createRoot } from 'react-dom/client';import { BrowserRouter,Navigate,Route,Routes } from 'react-router-dom';import './styles.css';import { AuthProvider,useAuth } from './contexts/AuthContext';import { ToastProvider } from './components/Toast';import Shell from './components/Shell';import CommandPalette from './components/CommandPalette';import { AuthPage,ResetPassword } from './pages/AuthPages';import Dashboard from './pages/Dashboard';import { Collection,ProjectDetail } from './pages/WorkspacePages';import Commands from './pages/Commands';import { Analytics,Settings } from './pages/ExtraPages';
function Protected({children}){const {user,loading}=useAuth();if(loading)return <div className="app-loading">Loading DevVault…</div>;return user?children:<Navigate to="/login" replace/>}function App(){const [palette,setPalette]=useState(false);useEffect(()=>{const f=e=>{if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='k'){e.preventDefault();setPalette(x=>!x)}};addEventListener('keydown',f);return()=>removeEventListener('keydown',f)},[]);const workspace=page=><Protected><Shell openPalette={()=>setPalette(true)}>{page}</Shell></Protected>;return <>{palette&&<CommandPalette onClose={()=>setPalette(false)}/>}<Routes><Route path="/login" element={<AuthPage mode="login"/>}/><Route path="/register" element={<AuthPage mode="register"/>}/><Route path="/forgot-password" element={<AuthPage mode="forgot"/>}/><Route path="/reset-password" element={<ResetPassword/>}/><Route path="/dashboard" element={workspace(<Dashboard/>)}/><Route path="/projects" element={workspace(<Collection type="projects"/>)}/><Route path="/projects/:id" element={workspace(<ProjectDetail/>)}/><Route path="/tasks" element={workspace(<Collection type="tasks"/>)}/><Route path="/snippets" element={workspace(<Collection type="snippets"/>)}/><Route path="/notes" element={workspace(<Collection type="notes"/>)}/><Route path="/commands" element={workspace(<Commands/>)}/><Route path="/analytics" element={workspace(<Analytics/>)}/><Route path="/settings" element={workspace(<Settings/>)}/><Route path="*" element={<Navigate to="/dashboard"/>}/></Routes></>};
if ('serviceWorker' in navigator) window.addEventListener('load', () => navigator.serviceWorker.register('/sw.js'));
createRoot(document.getElementById('root')).render(<BrowserRouter><AuthProvider><ToastProvider><App/></ToastProvider></AuthProvider></BrowserRouter>)
